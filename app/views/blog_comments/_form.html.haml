= form_for [@post,@blog_comment] do |f|
  - if @blog_comment.errors.any?
    #error_explanation
      %h2
        = pluralize(@blog_comment.errors.count, "error")
        prohibited this blog_comment from being saved:
      %ul
        - @blog_comment.errors.full_messages.each do |msg|
          %li= msg
  .field
    - unless current_user
      = "Guest"
    - else
      = current_user.email
  .field
    = f.label :body
    %br/
    = f.text_area :body
  .actions
    = f.submit "Create blog_comment", :class=>"btn"
